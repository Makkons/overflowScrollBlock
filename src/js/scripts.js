import OverFlowBlock from './overFlowBlock.js';

new OverFlowBlock({
    className: 'overflow-scroll-block',     // default class name for block
    visibleElements: 2,                     // number of visible elements
    offsetScroll: 0.2,                      // offset for scroll
})